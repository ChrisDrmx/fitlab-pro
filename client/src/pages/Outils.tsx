import { useState } from "react";

export default function Outils() { const [cm, setCm] = useState(0); const [mm, setMm] = useState(0); return <div className="page"><div className="page-header"><div><div className="eyebrow">Calculatrices</div><h1>Outils</h1><p className="subtle">Conversions rapides pendant la séance.</p></div></div><div className="grid-3"><section className="panel"><h2>Centimètres ↔ pouces</h2><div className="field" style={{ marginTop: 15 }}><label>Centimètres</label><input type="number" value={cm || ""} onChange={(e) => setCm(Number(e.target.value))} /></div><div className="notice">{cm ? `${(cm / 2.54).toFixed(2)} pouces` : "Saisis une longueur."}</div></section><section className="panel"><h2>Trace lie board</h2><div className="field" style={{ marginTop: 15 }}><label>Écart de trace en mm</label><input type="number" value={mm || ""} onChange={(e) => setMm(Number(e.target.value))} /></div><div className="notice">{mm ? `Environ ${(mm / 12).toFixed(1)}° — repère à confirmer` : "Saisis une trace."}</div></section><section className="panel"><h2>Déviation latérale</h2><p className="subtle">Repère pédagogique uniquement.</p><div className="notice">Environ 4 yards par degré de lie, selon le club et le swing.</div></section></div></div>; }
